<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ระบบสั่งอาหารออนไลน์</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500;600;700&display=swap');
        body { font-family: 'Kanit', sans-serif; }
        .fade-in { animation: fadeIn 0.3s ease-in; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
        .slide-up { animation: slideUp 0.3s ease-out; }
        @keyframes slideUp { from { transform: translateY(20px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
        .pulse-order { animation: pulse 2s infinite; }
        @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.7; } }
    </style>
</head>
<body class="bg-gradient-to-br from-orange-50 to-red-50 min-h-screen">
    <!-- หน้าเลือกโหมด -->
    <div id="modeSelection" class="min-h-screen flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl shadow-2xl p-8 max-w-md w-full text-center">
            <h1 class="text-3xl font-bold text-gray-800 mb-8">ระบบสั่งอาหารออนไลน์</h1>
            <div class="space-y-4">
                <button onclick="switchToCustomer()" class="w-full bg-gradient-to-r from-blue-500 to-blue-600 text-white py-4 px-6 rounded-xl font-semibold text-lg hover:from-blue-600 hover:to-blue-700 transition-all duration-300 transform hover:scale-105">
                    🍽️ หน้าลูกค้า
                </button>
                <button onclick="switchToOwner()" class="w-full bg-gradient-to-r from-green-500 to-green-600 text-white py-4 px-6 rounded-xl font-semibold text-lg hover:from-green-600 hover:to-green-700 transition-all duration-300 transform hover:scale-105">
                    👨‍💼 หน้าเจ้าของร้าน
                </button>
            </div>
        </div>
    </div>

    <!-- หน้าลูกค้า -->
    <div id="customerView" class="hidden">
        <!-- Header ลูกค้า -->
        <header class="bg-white shadow-lg sticky top-0 z-40">
            <div class="container mx-auto px-4 py-4">
                <div class="flex justify-between items-center">
                    <div>
                        <h1 id="restaurantName" class="text-2xl font-bold text-gray-800">ร้านอาหารอร่อย</h1>
                        <p class="text-gray-600">สั่งอาหารออนไลน์</p>
                    </div>
                    <div class="flex items-center space-x-4">
                        <div class="relative">
                            <button onclick="toggleCart()" class="bg-orange-500 text-white px-4 py-2 rounded-lg font-semibold hover:bg-orange-600 transition-colors">
                                🛒 ตรวจสอบออเดอร์ (<span id="cartCount">0</span>)
                            </button>
                        </div>
                        <button onclick="showOrderHistory()" class="bg-blue-500 text-white px-4 py-2 rounded-lg font-semibold hover:bg-blue-600 transition-colors">
                            📋 ประวัติการสั่งซื้อ
                        </button>
                        <button onclick="switchToModeSelection()" class="bg-gray-500 text-white px-4 py-2 rounded-lg font-semibold hover:bg-gray-600 transition-colors">
                            🏠 หน้าหลัก
                        </button>
                    </div>
                </div>
            </div>
        </header>

        <!-- เมนูอาหาร -->
        <main class="container mx-auto px-4 py-8">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6" id="menuGrid">
                <!-- เมนูจะถูกสร้างด้วย JavaScript -->
            </div>
        </main>

        <!-- ตะกร้าสินค้า Modal -->
        <div id="cartModal" class="hidden fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
            <div class="bg-white rounded-2xl max-w-2xl w-full max-h-[90vh] overflow-y-auto">
                <div class="p-6">
                    <div class="flex justify-between items-center mb-6">
                        <h2 class="text-2xl font-bold">ตะกร้าสินค้า</h2>
                        <button onclick="toggleCart()" class="text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
                    </div>
                    <div id="cartItems" class="space-y-4 mb-6">
                        <!-- รายการในตะกร้าจะแสดงที่นี่ -->
                    </div>
                    <div class="border-t pt-4">
                        <div class="flex justify-between items-center text-xl font-bold mb-4">
                            <span>ยอดรวม:</span>
                            <span id="totalPrice">0 บาท</span>
                        </div>
                        <button onclick="proceedToCheckout()" class="w-full bg-green-500 text-white py-3 rounded-lg font-semibold hover:bg-green-600 transition-colors">
                            ดำเนินการสั่งซื้อ
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- หน้าชำระเงิน -->
        <div id="checkoutModal" class="hidden fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
            <div class="bg-white rounded-2xl max-w-2xl w-full max-h-[90vh] overflow-y-auto">
                <div class="p-6">
                    <div class="flex justify-between items-center mb-6">
                        <h2 class="text-2xl font-bold">ชำระเงินและที่อยู่จัดส่ง</h2>
                        <button onclick="closeCheckout()" class="text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
                    </div>
                    
                    <form id="checkoutForm" class="space-y-6">
                        <div>
                            <h3 class="text-lg font-semibold mb-4">ที่อยู่จัดส่ง</h3>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <input type="text" id="customerName" placeholder="ชื่อ-นามสกุล" class="w-full p-3 border rounded-lg" required>
                                <input type="tel" id="customerPhone" placeholder="เบอร์โทรศัพท์" class="w-full p-3 border rounded-lg" required>
                                <textarea id="customerAddress" placeholder="ที่อยู่" class="w-full p-3 border rounded-lg col-span-2" rows="3" required></textarea>
                            </div>
                        </div>

                        <div>
                            <h3 class="text-lg font-semibold mb-4">วิธีการชำระเงิน</h3>
                            <div class="space-y-3">
                                <label class="flex items-center space-x-3 cursor-pointer">
                                    <input type="radio" name="paymentMethod" value="cash" class="w-4 h-4" checked>
                                    <span>💵 เงินสดปลายทาง</span>
                                </label>
                                <label class="flex items-center space-x-3 cursor-pointer">
                                    <input type="radio" name="paymentMethod" value="transfer" class="w-4 h-4">
                                    <span>🏦 โอนเงิน/สแกน QR Code</span>
                                </label>
                            </div>
                        </div>

                        <div id="transferSection" class="hidden">
                            <h4 class="font-semibold mb-3">แนบสลิปการโอนเงิน</h4>
                            <input type="file" id="paymentSlip" accept="image/*" class="w-full p-3 border rounded-lg">
                            <p class="text-sm text-gray-600 mt-2">กรุณาแนบสลิปการโอนเงิน</p>
                        </div>

                        <div class="border-t pt-4">
                            <div class="flex justify-between items-center text-xl font-bold mb-4">
                                <span>ยอดรวมทั้งหมด:</span>
                                <span id="checkoutTotal">0 บาท</span>
                            </div>
                            <button type="submit" class="w-full bg-green-500 text-white py-3 rounded-lg font-semibold hover:bg-green-600 transition-colors">
                                ยืนยันการสั่งซื้อ
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <!-- หน้าสถานะออเดอร์ -->
        <div id="orderStatusModal" class="hidden fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
            <div class="bg-white rounded-2xl max-w-md w-full">
                <div class="p-6 text-center">
                    <div class="mb-6">
                        <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
                            <span class="text-2xl">✅</span>
                        </div>
                        <h2 class="text-2xl font-bold text-green-600 mb-2">สั่งซื้อสำเร็จ!</h2>
                        <p class="text-gray-600">หมายเลขออเดอร์: <span id="orderNumber" class="font-bold"></span></p>
                    </div>
                    
                    <div class="mb-6">
                        <h3 class="font-semibold mb-3">สถานะปัจจุบัน</h3>
                        <div id="currentOrderStatus" class="bg-yellow-100 text-yellow-800 px-4 py-2 rounded-lg font-semibold">
                            รอการยืนยัน
                        </div>
                    </div>

                    <div class="space-y-3">
                        <button onclick="closeOrderStatus()" class="w-full bg-blue-500 text-white py-2 rounded-lg hover:bg-blue-600 transition-colors">
                            ปิด
                        </button>
                        <button onclick="showOrderHistory()" class="w-full bg-gray-500 text-white py-2 rounded-lg hover:bg-gray-600 transition-colors">
                            ดูประวัติการสั่งซื้อ
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- ประวัติการสั่งซื้อ -->
        <div id="orderHistoryModal" class="hidden fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
            <div class="bg-white rounded-2xl max-w-4xl w-full max-h-[90vh] overflow-y-auto">
                <div class="p-6">
                    <div class="flex justify-between items-center mb-6">
                        <h2 class="text-2xl font-bold">ประวัติการสั่งซื้อ</h2>
                        <button onclick="closeOrderHistory()" class="text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
                    </div>
                    <div id="orderHistoryList" class="space-y-4">
                        <!-- ประวัติออเดอร์จะแสดงที่นี่ -->
                    </div>
                </div>
            </div>
        </div>

        <!-- Modal แสดงสลิปขนาดใหญ่ -->
        <div id="slipModal" class="hidden fixed inset-0 bg-black bg-opacity-75 z-60 flex items-center justify-center p-4">
            <div class="relative max-w-3xl max-h-[90vh] overflow-auto">
                <button onclick="closeSlipModal()" class="absolute top-4 right-4 bg-white text-black rounded-full w-10 h-10 flex items-center justify-center text-xl font-bold hover:bg-gray-200 z-10">
                    &times;
                </button>
                <img id="slipImage" src="" alt="สลิปการโอนเงิน" class="max-w-full max-h-full rounded-lg">
            </div>
        </div>

        <!-- Modal แก้ไขเมนู -->
        <div id="editMenuModal" class="hidden fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
            <div class="bg-white rounded-2xl max-w-md w-full">
                <div class="p-6">
                    <div class="flex justify-between items-center mb-6">
                        <h2 class="text-2xl font-bold">แก้ไขเมนู</h2>
                        <button onclick="closeEditMenu()" class="text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
                    </div>
                    
                    <form id="editMenuForm" class="space-y-4">
                        <input type="hidden" id="editMenuId">
                        <div>
                            <label class="block text-sm font-semibold mb-2">ชื่อเมนู</label>
                            <input type="text" id="editMenuName" class="w-full p-3 border rounded-lg" required>
                        </div>
                        <div>
                            <label class="block text-sm font-semibold mb-2">รายละเอียด</label>
                            <textarea id="editMenuDescription" class="w-full p-3 border rounded-lg" rows="3"></textarea>
                        </div>
                        <div>
                            <label class="block text-sm font-semibold mb-2">ราคา (บาท)</label>
                            <input type="number" id="editMenuPrice" class="w-full p-3 border rounded-lg" required>
                        </div>
                        <div>
                            <label class="block text-sm font-semibold mb-2">รูปภาพใหม่ (ถ้าต้องการเปลี่ยน)</label>
                            <input type="file" id="editMenuImage" accept="image/*" class="w-full p-3 border rounded-lg">
                            <div id="currentImagePreview" class="mt-3">
                                <p class="text-sm text-gray-600 mb-2">รูปภาพปัจจุบัน:</p>
                                <img id="currentImage" src="" alt="รูปภาพปัจจุบัน" class="w-24 h-24 object-cover rounded-lg border">
                            </div>
                        </div>
                        <div class="flex space-x-3 pt-4">
                            <button type="submit" class="flex-1 bg-green-500 text-white py-3 rounded-lg font-semibold hover:bg-green-600 transition-colors">
                                บันทึกการแก้ไข
                            </button>
                            <button type="button" onclick="closeEditMenu()" class="flex-1 bg-gray-500 text-white py-3 rounded-lg font-semibold hover:bg-gray-600 transition-colors">
                                ยกเลิก
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- หน้าเจ้าของร้าน -->
    <div id="ownerView" class="hidden">
        <!-- Header เจ้าของร้าน -->
        <header class="bg-white shadow-lg sticky top-0 z-40">
            <div class="container mx-auto px-4 py-4">
                <div class="flex justify-between items-center">
                    <div>
                        <h1 class="text-2xl font-bold text-gray-800">จัดการร้านอาหาร</h1>
                        <p class="text-gray-600">ระบบจัดการสำหรับเจ้าของร้าน</p>
                    </div>
                    <div class="flex items-center space-x-4">
                        <div class="bg-green-100 px-4 py-2 rounded-lg">
                            <span class="text-green-800 font-semibold">ออเดอร์ใหม่: <span id="newOrderCount" class="pulse-order">0</span></span>
                        </div>
                        <button onclick="switchToModeSelection()" class="bg-gray-500 text-white px-4 py-2 rounded-lg font-semibold hover:bg-gray-600 transition-colors">
                            🏠 หน้าหลัก
                        </button>
                    </div>
                </div>
            </div>
        </header>

        <!-- แท็บจัดการ -->
        <div class="bg-white border-b">
            <div class="container mx-auto px-4">
                <div class="flex space-x-8">
                    <button onclick="showOwnerTab('orders')" class="owner-tab py-4 px-2 border-b-2 border-blue-500 text-blue-600 font-semibold">
                        📋 จัดการออเดอร์
                    </button>
                    <button onclick="showOwnerTab('menu')" class="owner-tab py-4 px-2 border-b-2 border-transparent text-gray-600 hover:text-gray-800">
                        🍽️ จัดการเมนู
                    </button>
                    <button onclick="showOwnerTab('restaurant')" class="owner-tab py-4 px-2 border-b-2 border-transparent text-gray-600 hover:text-gray-800">
                        🏪 ตั้งค่าร้าน
                    </button>
                </div>
            </div>
        </div>

        <!-- เนื้อหาแท็บ -->
        <main class="container mx-auto px-4 py-8">
            <!-- แท็บจัดการออเดอร์ -->
            <div id="ordersTab" class="owner-tab-content">
                <div class="bg-white rounded-lg shadow-lg p-6">
                    <h2 class="text-xl font-bold mb-6">รายการออเดอร์</h2>
                    <div id="ownerOrdersList" class="space-y-4">
                        <!-- รายการออเดอร์จะแสดงที่นี่ -->
                    </div>
                </div>
            </div>

            <!-- แท็บจัดการเมนู -->
            <div id="menuTab" class="owner-tab-content hidden">
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                    <!-- เพิ่มเมนูใหม่ -->
                    <div class="bg-white rounded-lg shadow-lg p-6">
                        <h2 class="text-xl font-bold mb-6">เพิ่มเมนูใหม่</h2>
                        <form id="addMenuForm" class="space-y-4">
                            <input type="text" id="menuName" placeholder="ชื่อเมนู" class="w-full p-3 border rounded-lg" required>
                            <textarea id="menuDescription" placeholder="รายละเอียด" class="w-full p-3 border rounded-lg" rows="3"></textarea>
                            <input type="number" id="menuPrice" placeholder="ราคา (บาท)" class="w-full p-3 border rounded-lg" required>
                            <input type="file" id="menuImage" accept="image/*" class="w-full p-3 border rounded-lg">
                            <button type="submit" class="w-full bg-green-500 text-white py-3 rounded-lg font-semibold hover:bg-green-600 transition-colors">
                                เพิ่มเมนู
                            </button>
                        </form>
                    </div>

                    <!-- รายการเมนูปัจจุบัน -->
                    <div class="bg-white rounded-lg shadow-lg p-6">
                        <h2 class="text-xl font-bold mb-6">เมนูปัจจุบัน</h2>
                        <div id="ownerMenuList" class="space-y-4">
                            <!-- รายการเมนูจะแสดงที่นี่ -->
                        </div>
                    </div>
                </div>
            </div>

            <!-- แท็บตั้งค่าร้าน -->
            <div id="restaurantTab" class="owner-tab-content hidden">
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                    <!-- ตั้งค่าร้าน -->
                    <div class="bg-white rounded-lg shadow-lg p-6">
                        <h2 class="text-xl font-bold mb-6">ตั้งค่าร้าน</h2>
                        <form id="restaurantSettingsForm" class="space-y-4">
                            <div>
                                <label class="block text-sm font-semibold mb-2">ชื่อร้าน</label>
                                <input type="text" id="restaurantNameInput" class="w-full p-3 border rounded-lg" required>
                            </div>
                            <button type="submit" class="bg-blue-500 text-white py-3 px-6 rounded-lg font-semibold hover:bg-blue-600 transition-colors">
                                บันทึกการเปลี่ยนแปลง
                            </button>
                        </form>
                    </div>

                    <!-- รหัสร้านสำหรับแชร์ -->
                    <div class="bg-white rounded-lg shadow-lg p-6">
                        <h2 class="text-xl font-bold mb-6">รหัสร้านสำหรับแชร์ข้อมูล</h2>
                        <div class="space-y-4">
                            <div class="bg-gray-50 p-4 rounded-lg border-2 border-dashed border-gray-300">
                                <p class="text-sm text-gray-600 mb-2">รหัสร้านของคุณ:</p>
                                <div class="flex items-center space-x-3">
                                    <code id="restaurantCode" class="text-2xl font-bold text-blue-600 bg-white px-4 py-2 rounded border">LOADING...</code>
                                    <button onclick="copyRestaurantCode()" class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600 transition-colors">
                                        📋 คัดลอก
                                    </button>
                                </div>
                            </div>
                            
                            <div class="text-sm text-gray-600">
                                <p class="mb-2"><strong>วิธีใช้งาน:</strong></p>
                                <ul class="list-disc list-inside space-y-1">
                                    <li>แชร์รหัสนี้ให้กับเครื่องอื่นๆ ที่ต้องการเข้าถึงข้อมูลร้านเดียวกัน</li>
                                    <li>เครื่องอื่นสามารถใส่รหัสนี้เพื่อซิงค์ข้อมูลร้าน</li>
                                    <li>ข้อมูลจะอัปเดตแบบเรียลไทม์ระหว่างเครื่อง</li>
                                </ul>
                            </div>

                            <div class="border-t pt-4">
                                <h3 class="font-semibold mb-3">เชื่อมต่อกับร้านอื่น</h3>
                                <div class="flex space-x-3 mb-3">
                                    <input type="text" id="connectRestaurantCode" placeholder="ใส่รหัสร้านที่ต้องการเชื่อมต่อ" class="flex-1 p-3 border rounded-lg">
                                    <button onclick="connectToRestaurant()" class="bg-orange-500 text-white px-6 py-3 rounded-lg hover:bg-orange-600 transition-colors">
                                        เชื่อมต่อ
                                    </button>
                                </div>
                                <div class="grid grid-cols-3 gap-2 mb-3">
                                    <button onclick="checkRestaurantCode()" class="bg-blue-500 text-white px-3 py-2 rounded-lg hover:bg-blue-600 transition-colors text-sm">
                                        🔍 ตรวจสอบรหัส
                                    </button>
                                    <button onclick="showAvailableCodes()" class="bg-gray-500 text-white px-3 py-2 rounded-lg hover:bg-gray-600 transition-colors text-sm">
                                        📋 ดูรหัสที่มี
                                    </button>
                                    <button onclick="testDeviceConnection()" class="bg-green-500 text-white px-3 py-2 rounded-lg hover:bg-green-600 transition-colors text-sm">
                                        📡 ทดสอบ
                                    </button>
                                </div>
                                <div id="codeCheckResult" class="hidden mb-3 p-3 rounded-lg border"></div>
                                <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-3 mb-3">
                                    <p class="text-yellow-800 text-sm">
                                        <strong>💡 วิธีใช้งาน:</strong><br>
                                        1. แชร์รหัสร้านให้อุปกรณ์อื่น<br>
                                        2. ใส่รหัสเดียวกันในอุปกรณ์อื่น<br>
                                        3. กดเชื่อมต่อ - ข้อมูลจะซิงค์กัน<br>
                                        4. ใช้ปุ่ม "ทดสอบ" เพื่อตรวจสอบการเชื่อมต่อ
                                    </p>
                                </div>
                                <p class="text-xs text-gray-500">⚠️ การเชื่อมต่อจะใช้ข้อมูลร้านเดียวกัน</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script>
        // ข้อมูลระบบ
        let currentView = 'modeSelection';
        let restaurantData = {};
        let cart = [];
        let orders = [];
        let orderCounter = 1000;
        let syncInterval;

        // ระบบเชื่อมต่อแบบง่าย
        let currentRestaurantCode = null;
        let cloudApiUrl = 'https://api.jsonbin.io/v3/b';
        let cloudApiKey = '$2a$10$YourAPIKeyHere'; // ใช้ API Key จริงสำหรับการใช้งานจริง
        
        // สร้างรหัสร้านแบบง่าย (4 หลัก)
        function generateSimpleCode() {
            return Math.floor(1000 + Math.random() * 9000).toString();
        }

        // ระบบคลาวด์จำลองด้วย localStorage + BroadcastChannel
        const cloudChannel = new BroadcastChannel('restaurant_sync');
        
        function saveToCloud() {
            try {
                const code = getOrCreateRestaurantCode();
                const cloudData = {
                    restaurantData: restaurantData,
                    orders: orders,
                    orderCounter: orderCounter,
                    lastUpdate: Date.now(),
                    code: code,
                    deviceId: getDeviceId()
                };
                
                // บันทึกในเครื่อง
                localStorage.setItem(`restaurant_${code}`, JSON.stringify(cloudData));
                localStorage.setItem('current_restaurant_code', code);
                
                // ส่งข้อมูลผ่าน BroadcastChannel (สำหรับแท็บเดียวกัน)
                cloudChannel.postMessage({
                    type: 'data_update',
                    code: code,
                    data: cloudData
                });
                
                // จำลองการส่งข้อมูลไปคลาวด์
                simulateCloudSync(code, cloudData);
                
                console.log('บันทึกข้อมูลสำเร็จ - รหัส:', code);
                
            } catch (error) {
                console.error('เกิดข้อผิดพลาดในการบันทึก:', error);
            }
        }

        function loadFromCloud() {
            try {
                const code = getOrCreateRestaurantCode();
                
                // ลองโหลดจากคลาวด์จำลองก่อน
                loadFromSimulatedCloud(code).then(cloudData => {
                    if (cloudData) {
                        restaurantData = cloudData.restaurantData || getDefaultRestaurantData();
                        orders = cloudData.orders || [];
                        orderCounter = cloudData.orderCounter || 1000;
                        
                        // บันทึกในเครื่อง
                        localStorage.setItem(`restaurant_${code}`, JSON.stringify(cloudData));
                        
                        console.log('โหลดจากคลาวด์สำเร็จ - รหัส:', code);
                        updateAllViews();
                    } else {
                        // ถ้าไม่มีในคลาวด์ ลองโหลดจากเครื่อง
                        loadFromLocalStorage(code);
                    }
                }).catch(() => {
                    // ถ้าเกิดข้อผิดพลาด ใช้ข้อมูลในเครื่อง
                    loadFromLocalStorage(code);
                });
                
            } catch (error) {
                console.error('เกิดข้อผิดพลาดในการโหลด:', error);
                loadFromLocalStorage(code);
            }
        }

        function loadFromLocalStorage(code) {
            const localData = localStorage.getItem(`restaurant_${code}`);
            
            if (localData) {
                const parsedData = JSON.parse(localData);
                restaurantData = parsedData.restaurantData || getDefaultRestaurantData();
                orders = parsedData.orders || [];
                orderCounter = parsedData.orderCounter || 1000;
                
                console.log('โหลดจากเครื่องสำเร็จ - รหัส:', code);
            } else {
                console.log('ไม่พบข้อมูล สร้างข้อมูลใหม่');
                restaurantData = getDefaultRestaurantData();
                orders = [];
                orderCounter = 1000;
                saveToCloud();
            }
        }

        // จำลองระบบคลาวด์ด้วย sessionStorage (แชร์ระหว่างแท็บ)
        function simulateCloudSync(code, data) {
            try {
                // ใช้ sessionStorage เป็นคลาวด์จำลอง
                const cloudKey = `cloud_restaurant_${code}`;
                sessionStorage.setItem(cloudKey, JSON.stringify(data));
                
                // จำลองการส่งข้อมูลไปเซิร์ฟเวอร์
                setTimeout(() => {
                    console.log('ส่งข้อมูลไปคลาวด์สำเร็จ (จำลอง)');
                }, 100);
                
            } catch (error) {
                console.error('เกิดข้อผิดพลาดในการซิงค์คลาวด์:', error);
            }
        }

        function loadFromSimulatedCloud(code) {
            return new Promise((resolve) => {
                try {
                    const cloudKey = `cloud_restaurant_${code}`;
                    const cloudData = sessionStorage.getItem(cloudKey);
                    
                    if (cloudData) {
                        resolve(JSON.parse(cloudData));
                    } else {
                        resolve(null);
                    }
                } catch (error) {
                    console.error('เกิดข้อผิดพลาดในการโหลดจากคลาวด์:', error);
                    resolve(null);
                }
            });
        }

        // สร้าง Device ID เฉพาะสำหรับแต่ละอุปกรณ์
        function getDeviceId() {
            let deviceId = localStorage.getItem('device_id');
            if (!deviceId) {
                deviceId = 'device_' + Math.random().toString(36).substr(2, 9);
                localStorage.setItem('device_id', deviceId);
            }
            return deviceId;
        }

        function getDefaultRestaurantData() {
            return {
                name: 'ร้านอาหารอร่อย',
                menu: [
                    {
                        id: 1,
                        name: 'ผัดไทย',
                        description: 'ผัดไทยรสชาติดั้งเดิม หอมหวานกำลังดี',
                        price: 60,
                        image: 'data:image/svg+xml;base64,'
                    },
                    {
                        id: 2,
                        name: 'ต้มยำกุ้ง',
                        description: 'ต้มยำกุ้งน้ำใส รสเปรื้อยจี๊ดจ๊าด',
                        price: 120,
                        image: 'data:image/svg+xml;base64,'
                    },
                    {
                        id: 3,
                        name: 'ข้าวผัดกุ้ง',
                        description: 'ข้าวผัดกุ้งสดใหม่ หอมกรุ่น',
                        price: 80,
                        image: 'data:image/svg+xml;base64,'
                    }
                ]
            };
        }

        // ฟังก์ชันสำหรับใช้แทน saveData() และ loadData()
        async function saveData() {
            await saveToCloud();
        }

        async function loadData() {
            await loadFromCloud();
        }

        // ตัวแปรสำหรับเก็บข้อมูลล่าสุด
        let lastKnownUpdate = 0;

        // ฟังก์ชันซิงค์ข้อมูลแบบง่าย
        function syncDataAcrossTabs() {
            try {
                const code = getOrCreateRestaurantCode();
                
                // ตรวจสอบจากคลาวด์จำลองก่อน
                loadFromSimulatedCloud(code).then(cloudData => {
                    if (cloudData && cloudData.lastUpdate > lastKnownUpdate) {
                        lastKnownUpdate = cloudData.lastUpdate;
                        
                        // อัปเดตข้อมูลในหน่วยความจำ
                        restaurantData = cloudData.restaurantData || restaurantData;
                        orders = cloudData.orders || orders;
                        orderCounter = cloudData.orderCounter || orderCounter;
                        
                        // บันทึกในเครื่อง
                        localStorage.setItem(`restaurant_${code}`, JSON.stringify(cloudData));
                        
                        console.log('ซิงค์จากคลาวด์:', new Date(cloudData.lastUpdate).toLocaleTimeString());
                        
                        // อัปเดตหน้าจอ
                        updateAllViews();
                        return;
                    }
                    
                    // ถ้าไม่มีข้อมูลใหม่จากคลาวด์ ตรวจสอบ localStorage
                    const localData = localStorage.getItem(`restaurant_${code}`);
                    
                    if (localData) {
                        const parsedData = JSON.parse(localData);
                        
                        // ตรวจสอบว่ามีการอัปเดตใหม่หรือไม่
                        if (parsedData.lastUpdate && parsedData.lastUpdate > lastKnownUpdate) {
                            lastKnownUpdate = parsedData.lastUpdate;
                            
                            // อัปเดตข้อมูลในหน่วยความจำ
                            restaurantData = parsedData.restaurantData || restaurantData;
                            orders = parsedData.orders || orders;
                            orderCounter = parsedData.orderCounter || orderCounter;
                            
                            console.log('ซิงค์จากเครื่อง:', new Date(parsedData.lastUpdate).toLocaleTimeString());
                            
                            // อัปเดตหน้าจอ
                            updateAllViews();
                        }
                    }
                }).catch(error => {
                    console.error('เกิดข้อผิดพลาดในการซิงค์:', error);
                });
                
            } catch (error) {
                console.error('เกิดข้อผิดพลาดในการซิงค์:', error);
            }
        }

        // ฟังก์ชันอัปเดตหน้าจอทั้งหมด
        function updateAllViews() {
            if (currentView === 'customer') {
                renderMenu();
                updateCartCount();
                updateRestaurantName();
            } else if (currentView === 'owner') {
                updateOwnerView();
                renderOwnerOrders();
                renderOwnerMenu();
                updateRestaurantName();
                
                // อัปเดตรหัสร้านในหน้าเจ้าของร้าน
                const codeElement = document.getElementById('restaurantCode');
                if (codeElement) {
                    codeElement.textContent = getOrCreateRestaurantCode();
                }
            }
        }

        // เริ่มระบบซิงค์อัตโนมัติแบบง่าย
        function startAutoSync() {
            if (syncInterval) clearInterval(syncInterval);
            
            // ตรวจสอบการเปลี่ยนแปลงทุก 2 วินาที
            syncInterval = setInterval(syncDataAcrossTabs, 2000);
            
            // ซิงค์ทันทีเมื่อเริ่มต้น
            syncDataAcrossTabs();
            
            // แสดงสถานะการเชื่อมต่อ
            showConnectionStatus();
        }

        // หยุดระบบซิงค์
        function stopAutoSync() {
            if (syncInterval) {
                clearInterval(syncInterval);
                syncInterval = null;
            }
        }

        // แสดงสถานะการเชื่อมต่อ
        function showConnectionStatus() {
            const code = getOrCreateRestaurantCode();
            const deviceId = getDeviceId();
            
            console.log('🔗 สถานะการเชื่อมต่อ:');
            console.log('📱 รหัสร้าน:', code);
            console.log('🆔 รหัสอุปกรณ์:', deviceId);
            console.log('💾 ระบบซิงค์: เปิดใช้งาน');
            
            // แสดงการแจ้งเตือนในหน้าจอ
            if (currentView === 'owner') {
                showNotification(`🔗 เชื่อมต่อแล้ว - รหัส: ${code}`, 'success');
            }
        }

        // ฟังก์ชันตรวจสอบการเชื่อมต่อระหว่างอุปกรณ์
        function testDeviceConnection() {
            const code = getOrCreateRestaurantCode();
            
            // สร้างข้อมูลทดสอบ
            const testData = {
                type: 'connection_test',
                timestamp: Date.now(),
                deviceId: getDeviceId(),
                message: 'ทดสอบการเชื่อมต่อ'
            };
            
            // บันทึกข้อมูลทดสอบ
            simulateCloudSync(code, {
                ...restaurantData,
                testConnection: testData,
                lastUpdate: Date.now()
            });
            
            console.log('📡 ส่งสัญญาณทดสอบการเชื่อมต่อ');
            showNotification('📡 ทดสอบการเชื่อมต่อ...', 'info');
            
            // ตรวจสอบผลลัพธ์หลัง 3 วินาที
            setTimeout(() => {
                loadFromSimulatedCloud(code).then(data => {
                    if (data && data.testConnection && data.testConnection.timestamp > testData.timestamp - 5000) {
                        showNotification('✅ การเชื่อมต่อทำงานปกติ!', 'success');
                    } else {
                        showNotification('⚠️ ไม่พบการตอบสนอง', 'error');
                    }
                });
            }, 3000);
        }

        // ฟังก์ชันสลับหน้า
        function switchToModeSelection() {
            document.getElementById('modeSelection').classList.remove('hidden');
            document.getElementById('customerView').classList.add('hidden');
            document.getElementById('ownerView').classList.add('hidden');
            currentView = 'modeSelection';
            saveData();
            stopAutoSync();
        }

        function switchToCustomer() {
            document.getElementById('modeSelection').classList.add('hidden');
            document.getElementById('customerView').classList.remove('hidden');
            document.getElementById('ownerView').classList.add('hidden');
            currentView = 'customer';
            saveData();
            updateRestaurantName();
            renderMenu();
            updateCartCount();
            startAutoSync();
        }

        function switchToOwner() {
            const password = prompt('กรุณาใส่รหัสผ่านเจ้าของร้าน:');
            if (password !== '1234') {
                showNotification('รหัสผ่านไม่ถูกต้อง!', 'error');
                return;
            }
            
            document.getElementById('modeSelection').classList.add('hidden');
            document.getElementById('customerView').classList.add('hidden');
            document.getElementById('ownerView').classList.remove('hidden');
            currentView = 'owner';
            saveData();
            showOwnerTab('orders');
            updateOwnerView();
            startAutoSync();
            showNotification('เข้าสู่ระบบเจ้าของร้านสำเร็จ!', 'success');
        }

        // ฟังก์ชันอัปเดตชื่อร้าน
        function updateRestaurantName() {
            document.getElementById('restaurantName').textContent = restaurantData.name;
            document.getElementById('restaurantNameInput').value = restaurantData.name;
        }

        // ฟังก์ชันแสดงเมนู
        function renderMenu() {
            const menuGrid = document.getElementById('menuGrid');
            menuGrid.innerHTML = '';

            restaurantData.menu.forEach(item => {
                const menuCard = document.createElement('div');
                menuCard.className = 'bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow duration-300 fade-in';
                menuCard.innerHTML = `
                    <img src="${item.image}" alt="${item.name}" class="w-full h-48 object-cover" onerror="this.src='data:image/svg+xml;base64,'; this.alt='รูปภาพไม่พร้อมใช้งาน';">
                    <div class="p-6">
                        <h3 class="text-xl font-bold mb-2">${item.name}</h3>
                        <p class="text-gray-600 mb-4">${item.description}</p>
                        <div class="flex justify-between items-center">
                            <span class="text-2xl font-bold text-orange-600">${item.price} บาท</span>
                            <button onclick="addToCart(${item.id})" class="bg-orange-500 text-white px-4 py-2 rounded-lg font-semibold hover:bg-orange-600 transition-colors">
                                เพิ่มลงตะกร้า
                            </button>
                        </div>
                    </div>
                `;
                menuGrid.appendChild(menuCard);
            });
        }

        // ฟังก์ชันจัดการตะกร้า
        function addToCart(itemId) {
            const item = restaurantData.menu.find(m => m.id === itemId);
            const existingItem = cart.find(c => c.id === itemId);
            
            if (existingItem) {
                existingItem.quantity += 1;
            } else {
                cart.push({...item, quantity: 1});
            }
            
            updateCartCount();
            showNotification('เพิ่มลงตะกร้าแล้ว!', 'success');
        }

        function updateCartCount() {
            const count = cart.reduce((sum, item) => sum + item.quantity, 0);
            document.getElementById('cartCount').textContent = count;
        }

        function toggleCart() {
            const modal = document.getElementById('cartModal');
            if (modal.classList.contains('hidden')) {
                renderCartItems();
                modal.classList.remove('hidden');
            } else {
                modal.classList.add('hidden');
            }
        }

        function renderCartItems() {
            const cartItems = document.getElementById('cartItems');
            const totalPrice = document.getElementById('totalPrice');
            
            if (cart.length === 0) {
                cartItems.innerHTML = '<p class="text-gray-500 text-center py-8">ตะกร้าว่าง</p>';
                totalPrice.textContent = '0 บาท';
                return;
            }

            cartItems.innerHTML = '';
            let total = 0;

            cart.forEach(item => {
                total += item.price * item.quantity;
                const cartItem = document.createElement('div');
                cartItem.className = 'flex justify-between items-center p-4 border rounded-lg';
                cartItem.innerHTML = `
                    <div class="flex-1">
                        <h4 class="font-semibold">${item.name}</h4>
                        <p class="text-gray-600">${item.price} บาท</p>
                    </div>
                    <div class="flex items-center space-x-3">
                        <button onclick="updateCartQuantity(${item.id}, -1)" class="bg-gray-200 text-gray-700 w-8 h-8 rounded-full hover:bg-gray-300">-</button>
                        <span class="font-semibold">${item.quantity}</span>
                        <button onclick="updateCartQuantity(${item.id}, 1)" class="bg-gray-200 text-gray-700 w-8 h-8 rounded-full hover:bg-gray-300">+</button>
                        <button onclick="removeFromCart(${item.id})" class="text-red-500 hover:text-red-700 ml-2">🗑️</button>
                    </div>
                `;
                cartItems.appendChild(cartItem);
            });

            totalPrice.textContent = `${total} บาท`;
        }

        function updateCartQuantity(itemId, change) {
            const item = cart.find(c => c.id === itemId);
            if (item) {
                item.quantity += change;
                if (item.quantity <= 0) {
                    removeFromCart(itemId);
                } else {
                    renderCartItems();
                    updateCartCount();
                }
            }
        }

        function removeFromCart(itemId) {
            cart = cart.filter(c => c.id !== itemId);
            renderCartItems();
            updateCartCount();
        }

        // ฟังก์ชันชำระเงิน
        function proceedToCheckout() {
            if (cart.length === 0) {
                showNotification('กรุณาเลือกสินค้าก่อน', 'error');
                return;
            }
            
            document.getElementById('cartModal').classList.add('hidden');
            document.getElementById('checkoutModal').classList.remove('hidden');
            
            const total = cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
            document.getElementById('checkoutTotal').textContent = `${total} บาท`;
        }

        function closeCheckout() {
            document.getElementById('checkoutModal').classList.add('hidden');
        }

        // จัดการวิธีการชำระเงิน
        document.addEventListener('DOMContentLoaded', function() {
            const paymentMethods = document.querySelectorAll('input[name="paymentMethod"]');
            const transferSection = document.getElementById('transferSection');
            
            paymentMethods.forEach(method => {
                method.addEventListener('change', function() {
                    if (this.value === 'transfer') {
                        transferSection.classList.remove('hidden');
                    } else {
                        transferSection.classList.add('hidden');
                    }
                });
            });

            // ฟอร์มชำระเงิน
            document.getElementById('checkoutForm').addEventListener('submit', function(e) {
                e.preventDefault();
                
                const paymentMethod = document.querySelector('input[name="paymentMethod"]:checked').value;
                const paymentSlip = document.getElementById('paymentSlip').files[0];
                
                if (paymentMethod === 'transfer' && !paymentSlip) {
                    showNotification('กรุณาแนบสลิปการโอนเงิน', 'error');
                    return;
                }
                
                // สร้างออเดอร์
                const orderNumber = `ORD${orderCounter++}`;
                const order = {
                    id: orderNumber,
                    items: [...cart],
                    total: cart.reduce((sum, item) => sum + (item.price * item.quantity), 0),
                    customerInfo: {
                        name: document.getElementById('customerName').value,
                        phone: document.getElementById('customerPhone').value,
                        address: document.getElementById('customerAddress').value
                    },
                    paymentMethod: paymentMethod,
                    paymentSlip: paymentSlip ? URL.createObjectURL(paymentSlip) : null,
                    status: 'รอการยืนยัน',
                    timestamp: new Date().toLocaleString('th-TH')
                };
                
                orders.push(order);
                cart = [];
                saveData(); // บันทึกข้อมูล
                
                // แสดงหน้าสถานะ
                document.getElementById('checkoutModal').classList.add('hidden');
                document.getElementById('orderNumber').textContent = orderNumber;
                document.getElementById('currentOrderStatus').textContent = 'รอการยืนยัน';
                document.getElementById('orderStatusModal').classList.remove('hidden');
                
                updateCartCount();
                updateOwnerView();
                
                // แจ้งเตือนออเดอร์ใหม่
                showNotification('สั่งซื้อสำเร็จ!', 'success');
                
                // เพิ่มเสียงแจ้งเตือน (ถ้าเบราว์เซอร์รองรับ)
                try {
                    const audio = new Audio('data:audio/wav;base64,UklGRnoGAABXQVZFZm10IBAAAAABAAEAQB8AAEAfAAABAAgAZGF0YQoGAACBhYqFbF1fdJivrJBhNjVgodDbq2EcBj+a2/LDciUFLIHO8tiJNwgZaLvt559NEAxQp+PwtmMcBjiR1/LMeSwFJHfH8N2QQAoUXrTp66hVFApGn+DyvmwhBSuBzvLZiTYIG2m98OScTgwOUarm7blmGgU7k9n1unEiBC13yO/eizEIHWq+8+OWT');
                    audio.play().catch(() => {}); // เล่นเสียงแจ้งเตือน
                } catch (e) {}
            });
        });

        // ฟังก์ชันสถานะออเดอร์
        function closeOrderStatus() {
            document.getElementById('orderStatusModal').classList.add('hidden');
        }

        function showOrderHistory() {
            document.getElementById('orderHistoryModal').classList.remove('hidden');
            renderOrderHistory();
        }

        function closeOrderHistory() {
            document.getElementById('orderHistoryModal').classList.add('hidden');
        }

        function renderOrderHistory() {
            const orderHistoryList = document.getElementById('orderHistoryList');
            
            if (orders.length === 0) {
                orderHistoryList.innerHTML = '<p class="text-gray-500 text-center py-8">ยังไม่มีประวัติการสั่งซื้อ</p>';
                return;
            }

            orderHistoryList.innerHTML = '';
            
            orders.forEach(order => {
                const orderCard = document.createElement('div');
                orderCard.className = 'bg-gray-50 rounded-lg p-6 border';
                orderCard.innerHTML = `
                    <div class="flex justify-between items-start mb-4">
                        <div>
                            <h3 class="font-bold text-lg">ออเดอร์ #${order.id}</h3>
                            <p class="text-gray-600">${order.timestamp}</p>
                        </div>
                        <div class="text-right">
                            <div class="px-3 py-1 rounded-full text-sm font-semibold ${getStatusColor(order.status)}">
                                ${order.status}
                            </div>
                            <p class="font-bold text-lg mt-2">${order.total} บาท</p>
                        </div>
                    </div>
                    <div class="space-y-2">
                        ${order.items.map(item => `
                            <div class="flex justify-between">
                                <span>${item.name} x${item.quantity}</span>
                                <span>${item.price * item.quantity} บาท</span>
                            </div>
                        `).join('')}
                    </div>
                    <div class="mt-4 pt-4 border-t">
                        <p><strong>ที่อยู่:</strong> ${order.customerInfo.address}</p>
                        <p><strong>เบอร์โทร:</strong> ${order.customerInfo.phone}</p>
                        <p><strong>การชำระเงิน:</strong> ${order.paymentMethod === 'cash' ? 'เงินสดปลายทาง' : 'โอนเงิน'}</p>
                        ${order.paymentSlip ? `
                            <div class="mt-3">
                                <p class="font-semibold mb-2">สลิปการโอนเงิน:</p>
                                <img src="${order.paymentSlip}" alt="สลิปการโอนเงิน" class="max-w-xs rounded-lg border cursor-pointer hover:shadow-lg transition-shadow" onclick="showSlipModal('${order.paymentSlip}')">
                                <p class="text-sm text-gray-600 mt-1">คลิกเพื่อดูขนาดใหญ่</p>
                            </div>
                        ` : ''}
                    </div>
                `;
                orderHistoryList.appendChild(orderCard);
            });
        }

        // ฟังก์ชันเจ้าของร้าน
        function showOwnerTab(tabName) {
            // ซ่อนแท็บทั้งหมด
            document.querySelectorAll('.owner-tab-content').forEach(tab => {
                tab.classList.add('hidden');
            });
            
            // แสดงแท็บที่เลือก
            document.getElementById(tabName + 'Tab').classList.remove('hidden');
            
            // อัปเดตสีแท็บ
            document.querySelectorAll('.owner-tab').forEach(tab => {
                tab.className = 'owner-tab py-4 px-2 border-b-2 border-transparent text-gray-600 hover:text-gray-800';
            });
            
            event.target.className = 'owner-tab py-4 px-2 border-b-2 border-blue-500 text-blue-600 font-semibold';
            
            // อัปเดตเนื้อหา
            if (tabName === 'orders') {
                renderOwnerOrders();
            } else if (tabName === 'menu') {
                renderOwnerMenu();
            }
        }

        function updateOwnerView() {
            const newOrderCount = orders.filter(order => order.status === 'รอการยืนยัน').length;
            document.getElementById('newOrderCount').textContent = newOrderCount;
            
            if (currentView === 'owner') {
                renderOwnerOrders();
                renderOwnerMenu();
            }
        }

        function renderOwnerOrders() {
            const ownerOrdersList = document.getElementById('ownerOrdersList');
            
            if (orders.length === 0) {
                ownerOrdersList.innerHTML = '<p class="text-gray-500 text-center py-8">ยังไม่มีออเดอร์</p>';
                return;
            }

            ownerOrdersList.innerHTML = '';
            
            orders.forEach(order => {
                const orderCard = document.createElement('div');
                orderCard.className = 'bg-gray-50 rounded-lg p-6 border';
                orderCard.innerHTML = `
                    <div class="flex justify-between items-start mb-4">
                        <div>
                            <h3 class="font-bold text-lg">ออเดอร์ #${order.id}</h3>
                            <p class="text-gray-600">${order.timestamp}</p>
                            <p class="font-semibold">ลูกค้า: ${order.customerInfo.name}</p>
                            <p>โทร: ${order.customerInfo.phone}</p>
                        </div>
                        <div class="text-right">
                            <p class="font-bold text-xl text-green-600">${order.total} บาท</p>
                            <p class="text-sm text-gray-600">${order.paymentMethod === 'cash' ? 'เงินสดปลายทาง' : 'โอนเงิน'}</p>
                        </div>
                    </div>
                    
                    <div class="mb-4">
                        <h4 class="font-semibold mb-2">รายการอาหาร:</h4>
                        <div class="space-y-1">
                            ${order.items.map(item => `
                                <div class="flex justify-between">
                                    <span>${item.name} x${item.quantity}</span>
                                    <span>${item.price * item.quantity} บาท</span>
                                </div>
                            `).join('')}
                        </div>
                    </div>
                    
                    <div class="mb-4">
                        <h4 class="font-semibold mb-2">ที่อยู่จัดส่ง:</h4>
                        <p>${order.customerInfo.address}</p>
                    </div>
                    
                    ${order.paymentSlip ? `
                        <div class="mb-4">
                            <h4 class="font-semibold mb-2">สลิปการโอนเงิน:</h4>
                            <img src="${order.paymentSlip}" alt="สลิปการโอนเงิน" class="max-w-xs rounded-lg border">
                        </div>
                    ` : ''}
                    
                    <div class="flex justify-between items-center pt-4 border-t">
                        <div class="flex items-center space-x-4">
                            <select onchange="updateOrderStatus('${order.id}', this.value)" class="px-3 py-2 border rounded-lg">
                                <option value="รอการยืนยัน" ${order.status === 'รอการยืนยัน' ? 'selected' : ''}>รอการยืนยัน</option>
                                <option value="กำลังทำ" ${order.status === 'กำลังทำ' ? 'selected' : ''}>กำลังทำ</option>
                                <option value="กำลังส่ง" ${order.status === 'กำลังส่ง' ? 'selected' : ''}>กำลังส่ง</option>
                                <option value="ส่งเสร็จแล้ว" ${order.status === 'ส่งเสร็จแล้ว' ? 'selected' : ''}>ส่งเสร็จแล้ว</option>
                            </select>
                            <div class="px-3 py-1 rounded-full text-sm font-semibold ${getStatusColor(order.status)}">
                                ${order.status}
                            </div>
                        </div>
                        <button onclick="deleteOrder('${order.id}')" class="bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600 transition-colors">
                            ลบออเดอร์
                        </button>
                    </div>
                `;
                ownerOrdersList.appendChild(orderCard);
            });
        }

        function updateOrderStatus(orderId, newStatus) {
            const order = orders.find(o => o.id === orderId);
            if (order) {
                order.status = newStatus;
                saveData(); // บันทึกข้อมูล
                updateOwnerView();
                showNotification('อัปเดตสถานะแล้ว', 'success');
            }
        }

        function deleteOrder(orderId) {
            if (confirm('ต้องการลบออเดอร์นี้หรือไม่?')) {
                orders = orders.filter(o => o.id !== orderId);
                saveData(); // บันทึกข้อมูล
                updateOwnerView();
                showNotification('ลบออเดอร์แล้ว', 'success');
            }
        }

        function getStatusColor(status) {
            switch(status) {
                case 'รอการยืนยัน': return 'bg-yellow-100 text-yellow-800';
                case 'กำลังทำ': return 'bg-blue-100 text-blue-800';
                case 'กำลังส่ง': return 'bg-purple-100 text-purple-800';
                case 'ส่งเสร็จแล้ว': return 'bg-green-100 text-green-800';
                default: return 'bg-gray-100 text-gray-800';
            }
        }

        // จัดการเมนู
        function renderOwnerMenu() {
            const ownerMenuList = document.getElementById('ownerMenuList');
            ownerMenuList.innerHTML = '';
            
            restaurantData.menu.forEach(item => {
                const menuItem = document.createElement('div');
                menuItem.className = 'flex justify-between items-center p-4 border rounded-lg';
                menuItem.innerHTML = `
                    <div class="flex items-center space-x-4">
                        <img src="${item.image}" alt="${item.name}" class="w-16 h-16 object-cover rounded-lg" onerror="this.src='data:image/svg+xml;base64,'; this.alt='รูปภาพไม่พร้อมใช้งาน';">
                        <div>
                            <h4 class="font-semibold">${item.name}</h4>
                            <p class="text-gray-600 text-sm">${item.description}</p>
                            <p class="font-bold text-orange-600">${item.price} บาท</p>
                        </div>
                    </div>
                    <div class="flex space-x-2">
                        <button onclick="editMenuItem(${item.id})" class="bg-blue-500 text-white px-3 py-1 rounded hover:bg-blue-600 transition-colors">
                            แก้ไข
                        </button>
                        <button onclick="deleteMenuItem(${item.id})" class="bg-red-500 text-white px-3 py-1 rounded hover:bg-red-600 transition-colors">
                            ลบ
                        </button>
                    </div>
                `;
                ownerMenuList.appendChild(menuItem);
            });
        }

        function editMenuItem(itemId) {
            const item = restaurantData.menu.find(m => m.id === itemId);
            if (!item) return;
            
            // เติมข้อมูลในฟอร์มแก้ไข
            document.getElementById('editMenuId').value = item.id;
            document.getElementById('editMenuName').value = item.name;
            document.getElementById('editMenuDescription').value = item.description;
            document.getElementById('editMenuPrice').value = item.price;
            document.getElementById('currentImage').src = item.image;
            
            // แสดง Modal
            document.getElementById('editMenuModal').classList.remove('hidden');
        }

        function closeEditMenu() {
            document.getElementById('editMenuModal').classList.add('hidden');
            document.getElementById('editMenuForm').reset();
        }

        function deleteMenuItem(itemId) {
            if (confirm('ต้องการลบเมนูนี้หรือไม่?')) {
                restaurantData.menu = restaurantData.menu.filter(item => item.id !== itemId);
                saveData(); // บันทึกข้อมูล
                renderOwnerMenu();
                if (currentView === 'customer') {
                    renderMenu();
                }
                showNotification('ลบเมนูแล้ว', 'success');
            }
        }

        // เพิ่มเมนูใหม่
        document.addEventListener('DOMContentLoaded', function() {
            document.getElementById('addMenuForm').addEventListener('submit', function(e) {
                e.preventDefault();
                
                const name = document.getElementById('menuName').value;
                const description = document.getElementById('menuDescription').value;
                const price = parseInt(document.getElementById('menuPrice').value);
                const imageFile = document.getElementById('menuImage').files[0];
                
                let imageUrl = 'data:image/svg+xml;base64,';
                
                if (imageFile) {
                    imageUrl = URL.createObjectURL(imageFile);
                }
                
                const newItem = {
                    id: Date.now(),
                    name: name,
                    description: description,
                    price: price,
                    image: imageUrl
                };
                
                restaurantData.menu.push(newItem);
                saveData(); // บันทึกข้อมูล
                
                // รีเซ็ตฟอร์ม
                this.reset();
                
                renderOwnerMenu();
                if (currentView === 'customer') {
                    renderMenu();
                }
                showNotification('เพิ่มเมนูใหม่แล้ว', 'success');
            });

            // ตั้งค่าร้าน
            document.getElementById('restaurantSettingsForm').addEventListener('submit', function(e) {
                e.preventDefault();
                
                const newName = document.getElementById('restaurantNameInput').value;
                restaurantData.name = newName;
                saveData(); // บันทึกข้อมูล
                
                updateRestaurantName();
                showNotification('บันทึกการเปลี่ยนแปลงแล้ว', 'success');
            });

            // ฟอร์มแก้ไขเมนู
            document.getElementById('editMenuForm').addEventListener('submit', function(e) {
                e.preventDefault();
                
                const itemId = parseInt(document.getElementById('editMenuId').value);
                const name = document.getElementById('editMenuName').value;
                const description = document.getElementById('editMenuDescription').value;
                const price = parseInt(document.getElementById('editMenuPrice').value);
                const imageFile = document.getElementById('editMenuImage').files[0];
                
                // หาเมนูที่ต้องการแก้ไข
                const itemIndex = restaurantData.menu.findIndex(item => item.id === itemId);
                if (itemIndex === -1) return;
                
                // อัปเดตข้อมูล
                restaurantData.menu[itemIndex].name = name;
                restaurantData.menu[itemIndex].description = description;
                restaurantData.menu[itemIndex].price = price;
                
                // อัปเดตรูปภาพถ้ามีการเลือกใหม่
                if (imageFile) {
                    restaurantData.menu[itemIndex].image = URL.createObjectURL(imageFile);
                }
                
                saveData(); // บันทึกข้อมูล
                
                // ปิด Modal และรีเซ็ตฟอร์ม
                closeEditMenu();
                
                // อัปเดตหน้าจอ
                renderOwnerMenu();
                if (currentView === 'customer') {
                    renderMenu();
                }
                
                showNotification('แก้ไขเมนูสำเร็จ!', 'success');
            });
        });

        // ฟังก์ชันแสดงสลิปขนาดใหญ่
        function showSlipModal(slipUrl) {
            document.getElementById('slipImage').src = slipUrl;
            document.getElementById('slipModal').classList.remove('hidden');
        }

        function closeSlipModal() {
            document.getElementById('slipModal').classList.add('hidden');
        }

        // ฟังก์ชันแสดงการแจ้งเตือน
        function showNotification(message, type = 'info') {
            const notification = document.createElement('div');
            notification.className = `fixed top-4 right-4 z-50 px-6 py-3 rounded-lg text-white font-semibold slide-up ${
                type === 'success' ? 'bg-green-500' : 
                type === 'error' ? 'bg-red-500' : 'bg-blue-500'
            }`;
            notification.textContent = message;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.remove();
            }, 3000);
        }

        // ระบบรหัสร้านแบบง่าย
        function getOrCreateRestaurantCode() {
            // ใช้รหัสปัจจุบันถ้ามี
            if (currentRestaurantCode) {
                return currentRestaurantCode;
            }
            
            let code = localStorage.getItem('current_restaurant_code');
            if (!code) {
                code = generateSimpleCode();
                localStorage.setItem('current_restaurant_code', code);
            }
            
            currentRestaurantCode = code;
            return code;
        }

        // เริ่มต้นระบบ
        function initializeSystem() {
            try {
                // โหลดรหัสร้านก่อน
                currentRestaurantCode = localStorage.getItem('current_restaurant_code');
                if (!currentRestaurantCode) {
                    currentRestaurantCode = generateSimpleCode();
                    localStorage.setItem('current_restaurant_code', currentRestaurantCode);
                }
                
                // โหลดข้อมูล
                loadData();
                
                // อัปเดตหน้าจอ
                updateRestaurantName();
                
                // แสดงรหัสร้านในหน้าเจ้าของร้าน
                setTimeout(() => {
                    const codeElement = document.getElementById('restaurantCode');
                    if (codeElement) {
                        codeElement.textContent = currentRestaurantCode;
                    }
                }, 100);
                
                console.log('ระบบเริ่มต้นสำเร็จ - รหัสร้าน:', currentRestaurantCode);
                
            } catch (error) {
                console.error('เกิดข้อผิดพลาดในการเริ่มต้นระบบ:', error);
            }
        }

        // ฟังก์ชันคัดลอกรหัสร้าน
        function copyRestaurantCode() {
            const code = document.getElementById('restaurantCode').textContent;
            navigator.clipboard.writeText(code).then(() => {
                showNotification('คัดลอกรหัสร้านแล้ว!', 'success');
            }).catch(() => {
                // Fallback สำหรับเบราว์เซอร์เก่า
                const textArea = document.createElement('textarea');
                textArea.value = code;
                document.body.appendChild(textArea);
                textArea.select();
                document.execCommand('copy');
                document.body.removeChild(textArea);
                showNotification('คัดลอกรหัสร้านแล้ว!', 'success');
            });
        }

        // ฟังก์ชันเชื่อมต่อกับร้านอื่น
        function connectToRestaurant() {
            const inputCode = document.getElementById('connectRestaurantCode').value.trim();
            
            if (!inputCode) {
                showNotification('กรุณาใส่รหัสร้าน', 'error');
                return;
            }

            if (!confirm('การเชื่อมต่อจะใช้ข้อมูลร้านเดียวกัน ต้องการดำเนินการต่อหรือไม่?')) {
                return;
            }

            try {
                // หยุดการซิงค์ปัจจุบัน
                stopAutoSync();
                
                // ค้นหาข้อมูลของรหัสร้านที่ต้องการเชื่อมต่อ
                const targetData = localStorage.getItem(`restaurant_${inputCode}`);
                
                if (targetData) {
                    // พบข้อมูล - โหลดข้อมูลจากรหัสร้านเป้าหมาย
                    const parsedData = JSON.parse(targetData);
                    
                    // เปลี่ยนรหัสร้าน
                    currentRestaurantCode = inputCode;
                    localStorage.setItem('current_restaurant_code', inputCode);
                    
                    // อัปเดตข้อมูลในหน่วยความจำ
                    restaurantData = parsedData.restaurantData || getDefaultRestaurantData();
                    orders = parsedData.orders || [];
                    orderCounter = parsedData.orderCounter || 1000;
                    
                    // รีเซ็ตข้อมูลการซิงค์
                    lastKnownUpdate = parsedData.lastUpdate || 0;
                    
                    // บันทึกข้อมูลใหม่
                    saveToCloud();
                    
                    // เริ่มการซิงค์ใหม่
                    startAutoSync();
                    
                    // อัปเดตหน้าจอ
                    document.getElementById('restaurantCode').textContent = inputCode;
                    document.getElementById('connectRestaurantCode').value = '';
                    updateAllViews();
                    
                    showNotification(`เชื่อมต่อสำเร็จ! ร้าน: ${restaurantData.name}`, 'success');
                    console.log('เชื่อมต่อสำเร็จ - รหัส:', inputCode);
                    
                } else {
                    // ไม่พบข้อมูล - สร้างข้อมูลใหม่สำหรับรหัสนี้
                    console.log('ไม่พบข้อมูลสำหรับรหัส:', inputCode, 'สร้างข้อมูลใหม่');
                    
                    // เปลี่ยนรหัสร้าน
                    currentRestaurantCode = inputCode;
                    localStorage.setItem('current_restaurant_code', inputCode);
                    
                    // ใช้ข้อมูลเริ่มต้น
                    restaurantData = getDefaultRestaurantData();
                    orders = [];
                    orderCounter = 1000;
                    
                    // รีเซ็ตข้อมูลการซิงค์
                    lastKnownUpdate = 0;
                    
                    // บันทึกข้อมูลใหม่
                    saveToCloud();
                    
                    // เริ่มการซิงค์ใหม่
                    startAutoSync();
                    
                    // อัปเดตหน้าจอ
                    document.getElementById('restaurantCode').textContent = inputCode;
                    document.getElementById('connectRestaurantCode').value = '';
                    updateAllViews();
                    
                    showNotification('เชื่อมต่อสำเร็จ! สร้างร้านใหม่สำหรับรหัสนี้', 'success');
                }
                
            } catch (error) {
                console.error('เกิดข้อผิดพลาดในการเชื่อมต่อ:', error);
                showNotification('เกิดข้อผิดพลาดในการเชื่อมต่อ กรุณาลองใหม่', 'error');
                
                // กู้คืนรหัสเดิม
                const oldCode = localStorage.getItem('current_restaurant_code');
                if (oldCode && oldCode !== inputCode) {
                    currentRestaurantCode = oldCode;
                    startAutoSync();
                }
            }
        }

        // ฟังก์ชันตรวจสอบรหัสร้าน
        function checkRestaurantCode() {
            const inputCode = document.getElementById('connectRestaurantCode').value.trim();
            const resultDiv = document.getElementById('codeCheckResult');
            
            if (!inputCode) {
                showNotification('กรุณาใส่รหัสร้าน', 'error');
                return;
            }
            
            // ค้นหาข้อมูลของรหัสร้าน
            const targetData = localStorage.getItem(`restaurant_${inputCode}`);
            
            resultDiv.classList.remove('hidden');
            
            if (targetData) {
                try {
                    const parsedData = JSON.parse(targetData);
                    const lastUpdate = parsedData.lastUpdate ? new Date(parsedData.lastUpdate).toLocaleString('th-TH') : 'ไม่ทราบ';
                    
                    resultDiv.className = 'mb-3 p-3 rounded-lg border bg-green-50 border-green-200';
                    resultDiv.innerHTML = `
                        <div class="text-green-800">
                            <p class="font-semibold">✅ พบข้อมูลร้าน!</p>
                            <p><strong>ชื่อร้าน:</strong> ${parsedData.restaurantData?.name || 'ไม่ระบุ'}</p>
                            <p><strong>จำนวนเมนู:</strong> ${parsedData.restaurantData?.menu?.length || 0} รายการ</p>
                            <p><strong>จำนวนออเดอร์:</strong> ${parsedData.orders?.length || 0} รายการ</p>
                            <p><strong>อัปเดตล่าสุด:</strong> ${lastUpdate}</p>
                        </div>
                    `;
                } catch (e) {
                    resultDiv.className = 'mb-3 p-3 rounded-lg border bg-red-50 border-red-200';
                    resultDiv.innerHTML = `
                        <div class="text-red-800">
                            <p class="font-semibold">❌ ข้อมูลเสียหาย</p>
                            <p>พบรหัสแต่ข้อมูลไม่สามารถอ่านได้</p>
                        </div>
                    `;
                }
            } else {
                resultDiv.className = 'mb-3 p-3 rounded-lg border bg-yellow-50 border-yellow-200';
                resultDiv.innerHTML = `
                    <div class="text-yellow-800">
                        <p class="font-semibold">⚠️ ไม่พบข้อมูล</p>
                        <p>รหัส "${inputCode}" ยังไม่มีข้อมูล</p>
                        <p class="text-sm mt-1">การเชื่อมต่อจะสร้างร้านใหม่สำหรับรหัสนี้</p>
                    </div>
                `;
            }
        }
        
        // ฟังก์ชันแสดงรหัสที่มีอยู่
        function showAvailableCodes() {
            const resultDiv = document.getElementById('codeCheckResult');
            const availableCodes = [];
            
            // ค้นหารหัสทั้งหมดที่มีอยู่
            for (let i = 0; i < localStorage.length; i++) {
                const key = localStorage.key(i);
                if (key && key.startsWith('restaurant_')) {
                    const data = localStorage.getItem(key);
                    if (data) {
                        try {
                            const parsed = JSON.parse(data);
                            const code = key.replace('restaurant_', '');
                            if (parsed.restaurantData) {
                                availableCodes.push({
                                    code: code,
                                    name: parsed.restaurantData.name,
                                    menuCount: parsed.restaurantData.menu?.length || 0,
                                    orderCount: parsed.orders?.length || 0,
                                    lastUpdate: parsed.lastUpdate ? new Date(parsed.lastUpdate).toLocaleString('th-TH') : 'ไม่ทราบ'
                                });
                            }
                        } catch (e) {}
                    }
                }
            }
            
            resultDiv.classList.remove('hidden');
            
            if (availableCodes.length > 0) {
                resultDiv.className = 'mb-3 p-3 rounded-lg border bg-blue-50 border-blue-200';
                resultDiv.innerHTML = `
                    <div class="text-blue-800">
                        <p class="font-semibold mb-3">📋 รหัสร้านที่มีอยู่ (${availableCodes.length} ร้าน)</p>
                        <div class="space-y-2 max-h-40 overflow-y-auto">
                            ${availableCodes.map(code => `
                                <div class="bg-white p-2 rounded border text-sm">
                                    <div class="flex justify-between items-start">
                                        <div>
                                            <p class="font-semibold text-lg">${code.code}</p>
                                            <p>${code.name}</p>
                                            <p class="text-xs text-gray-600">เมนู: ${code.menuCount} | ออเดอร์: ${code.orderCount}</p>
                                        </div>
                                        <button onclick="document.getElementById('connectRestaurantCode').value='${code.code}'" 
                                                class="bg-blue-500 text-white px-2 py-1 rounded text-xs hover:bg-blue-600">
                                            เลือก
                                        </button>
                                    </div>
                                </div>
                            `).join('')}
                        </div>
                    </div>
                `;
            } else {
                resultDiv.className = 'mb-3 p-3 rounded-lg border bg-gray-50 border-gray-200';
                resultDiv.innerHTML = `
                    <div class="text-gray-800">
                        <p class="font-semibold">📋 ไม่มีรหัสร้านอื่น</p>
                        <p>ยังไม่มีข้อมูลร้านอื่นในเครื่องนี้</p>
                    </div>
                `;
            }
        }

        // เรียกใช้ระบบเริ่มต้น
        initializeSystem();
        
        // ฟังการเปลี่ยนแปลงใน localStorage จากแท็บอื่น
        window.addEventListener('storage', function(e) {
            // ตรวจสอบว่าเป็นการเปลี่ยนแปลงของร้านปัจจุบันหรือไม่
            const code = getOrCreateRestaurantCode();
            if (e.key === `restaurant_${code}`) {
                console.log('ตรวจพบการเปลี่ยนแปลงจาก localStorage');
                syncDataAcrossTabs();
            }
        });

        // ฟัง BroadcastChannel สำหรับการซิงค์ข้อมูล
        cloudChannel.addEventListener('message', function(event) {
            const { type, code, data } = event.data;
            const currentCode = getOrCreateRestaurantCode();
            
            if (type === 'data_update' && code === currentCode) {
                console.log('ได้รับข้อมูลจาก BroadcastChannel');
                
                if (data.lastUpdate > lastKnownUpdate) {
                    lastKnownUpdate = data.lastUpdate;
                    
                    // อัปเดตข้อมูลในหน่วยความจำ
                    restaurantData = data.restaurantData || restaurantData;
                    orders = data.orders || orders;
                    orderCounter = data.orderCounter || orderCounter;
                    
                    // อัปเดตหน้าจอ
                    updateAllViews();
                    
                    console.log('ซิงค์ข้อมูลจาก BroadcastChannel สำเร็จ');
                }
            }
        });

        // ฟังก์ชันแจ้งเตือนเมื่อมีอุปกรณ์ใหม่เชื่อมต่อ
        function notifyDeviceConnection(deviceId) {
            if (currentView === 'owner') {
                showNotification(`📱 อุปกรณ์ใหม่เชื่อมต่อ: ${deviceId.substr(-6)}`, 'info');
            }
        }

        // ตรวจสอบการเชื่อมต่ออุปกรณ์ใหม่
        setInterval(() => {
            const code = getOrCreateRestaurantCode();
            loadFromSimulatedCloud(code).then(data => {
                if (data && data.deviceId && data.deviceId !== getDeviceId()) {
                    // มีอุปกรณ์อื่นใช้รหัสเดียวกัน
                    const lastSeen = localStorage.getItem(`last_seen_${data.deviceId}`);
                    if (!lastSeen || Date.now() - parseInt(lastSeen) > 60000) {
                        localStorage.setItem(`last_seen_${data.deviceId}`, Date.now().toString());
                        notifyDeviceConnection(data.deviceId);
                    }
                }
            });
        }, 10000); // ตรวจสอบทุก 10 วินาที
    </script>
<script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'97b45197d1c5fcef',t:'MTc1NzIyODMxNy4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>
